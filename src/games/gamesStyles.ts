import { StyleSheet } from 'react-native';
export const gamesStyles = StyleSheet.create({
    gameButton: {
        padding: 15,
        backgroundColor: 'blue',
        borderRadius: 10,
        alignItems: 'center',
        marginTop: 20,
    },
})